# What Would Camus Say — 产品规划与 Agent 执行说明

> 文档版本：v0.2
> 产品阶段：MVP 规划  
> 首发语言：简体中文与英文（输入界面固定英文；回答按用户问题自动选择中文或英文，不提供手动开关）
> 目标形态：可公网访问的纯静态 Web 应用  
> 主要读者：产品、内容与开发 Agent

## 1. Agent 必读摘要

本项目要做的是一个“加缪思想推演器”，不是复活、克隆或冒充加缪。

用户输入现实生活问题，系统检索与问题最相关的加缪思想卡片，以确定性规则组织为一段有出处、有边界、有行动价值的回答。MVP 运行时不调用任何大模型 API，后端也不运行 AI 模型。

所有设计与实现必须遵守以下优先级：

1. 加缪思想的准确性。
2. 回答与用户问题的相关性。
3. 回答的诚实性、边界与可行动性。
4. 清醒、克制、简洁的表达风格。
5. 与加缪文字气质的相似度。

当“像加缪说话”与“准确表达加缪思想”冲突时，永远选择后者。

MVP 的核心技术决策已经确定：

- 使用静态托管，不设置 AI API、推理服务或数据库。
- 使用浏览器内双语 BM25 检索，不在 MVP 中使用语义向量模型。
- 使用人工审核的思想卡片，不把原著全文或现代中文译本打包进产品。
- 使用确定性的回答结构和文案组件，不让生成模型自由写答案。
- 不提供账号、长期记忆、亲密度、陪伴养成、虚拟关系或主动召回。
- 不声称“我是加缪”，不把系统生成内容标成“加缪原话”。
- 所有用户问题默认只在浏览器内处理，不上传、不保存。

Agent 不得在未更新本规划并获得产品确认的情况下，用 RAG、大模型 API、浏览器 LLM、聊天记忆或人物角色扮演替换上述 MVP。

## 2. 产品定义

### 2.1 一句话描述

What Would Camus Say 是一个帮助用户从加缪思想出发重新审视现实困境的网页工具。

### 2.2 用户价值

用户不需要了解“荒诞”“反抗”“限度”等哲学术语，只需要描述自己的困境。产品负责找到相关思想、说明其边界，并提供一个可以继续思考或采取行动的方向。

### 2.3 产品不是

- 不是加缪本人或加缪数字复活。
- 不是虚拟伴侣、心理治疗师或持续陪伴服务。
- 不是原著搜索器、名言生成器或文学仿写器。
- 不是万能聊天机器人。
- 不是医疗、心理、法律、财务或危机干预专业服务。
- 不是对“加缪在今天一定会怎样回答”的事实性断言。

### 2.4 推荐产品表述

统一使用以下表述：

> 回答正文是基于加缪作品与思想研究的系统推演，不代表加缪本人；结尾短引句来自独立审核的作品引文库，页面只显示书名，版本与译者信息保留在数据层。

避免使用：

- “加缪回答你”
- “和加缪聊天”
- “复活加缪”
- “加缪本人会说”
- “你的加缪人生导师”

可以使用：

- “从加缪思想看”
- “加缪的作品提供了一种这样的视角”
- “如果沿着加缪的思想继续推演”
- “What Would Camus Say”

## 3. 目标用户与核心场景

### 3.1 首要用户

- 对哲学感兴趣，但未系统阅读加缪的人。
- 正在面对工作、选择、关系、意义与不公问题，希望获得另一种思考框架的人。
- 读过加缪，希望把他的思想用于现实生活的人。

### 3.2 核心用户任务

> 当我遇到一个难以判断的人生问题时，我希望知道从加缪思想出发应如何理解它，以便形成自己的判断，而不是得到替我做决定的答案。

### 3.3 首批问题主题

MVP 覆盖以下主题：

1. 意义、荒诞与虚无感。
2. 工作、重复劳动与职业倦怠。
3. 自由、选择与责任。
4. 反抗、不公与服从。
5. 限度、节制与手段。
6. 孤独、关系与团结。
7. 希望、失望与现实。
8. 幸福、当下与感官生活。
9. 失败、痛苦与死亡意识。
10. 道德冲突与个人良知。
11. 社会角色、评价与自我判断。
12. 行动、妥协与后果。

## 4. MVP 范围

### 4.1 MVP 必须包含

#### A. 问题输入

- 一个主要文本输入框。
- 支持 10–300 个中英文字符。
- 首页与问题输入界面固定使用英文，不因输入内容发生实时切换。
- 提交后自动判断问题语言，并让检索、回答与结果页使用对应的中文或英文。
- 不提供手动语言开关；极短或语言不明确的问题默认使用英文回答。
- 提供 4–6 个示例问题。
- 输入为空、过短或明显无意义时给出具体提示。
- 点击首页入口后进入独立的全屏提问视图，不使用抽屉、弹窗或聊天气泡；页面只保留返回、问题说明、输入、提交与低权重示例。
- 提交后不跳转到传统聊天界面，而进入单次“思想推演结果”。

#### B. 本地 BM25 检索

- 所有检索在浏览器内完成。
- 对问题按检测语言进行分词、标准化和有限同义词扩展。
- 检索标题、原则、关键词、别名、适用场景与边界字段。
- 返回一个主思想卡片和最多两个辅助卡片。
- 检索置信度不足时明确说“暂未找到足够贴切的思想依据”，不得拼凑伪答案。

#### C. 确定性回答拼装

回答在数据层固定包含以下语义组件：

1. **看见困境**：只概括用户问题中的核心张力，不在回答正文复述原问题文本。
2. **加缪视角**：使用主思想卡片说明核心判断。
3. **必要边界**：说明该思想不意味着什么，避免极端化。
4. **现实一步**：给出一个低风险、可执行的下一步。
5. **留给你的问题**：在数据层保留一个帮助用户形成自主判断的问题，供测试与后续产品使用。

结果页不显示上述编号和分段标题，而是把前四项整理为一段自然文字。结尾视觉引句必须从独立审核的直接引文库选择；可以使用法文原文或有明确译者记录的英文译本短引文，并按回答语言显示译文。界面只显示作者与书名，版本、译者和语言信息保留在数据层；不得把第五项反思问题冒充或显示成加缪原话。

回答目标长度为 180–450 个中文字符。不得输出空泛鸡汤，不替用户做重大决定。

#### D. 思想来源与透明度

- 来源元数据保留在卡片库与方法页，不在单次结果页重复展示。
- 直接引语必须来自单独审核的引文库，包含来源版本文本、来源语言、译者或版本信息、作品名和权利状态；结果页只显示作品名。
- 非直接引语不得使用引号，也不得写“加缪说过”。
- 页面固定显示产品说明：这是系统推演，不代表加缪本人。
- 单次结果页保持精简，不展示检索命中解释、相关主题或反馈控件。

#### E. 安全分流

- 对自残、自杀、正在遭受暴力、重大财产欺诈等极端情境使用独立规则处理。
- 极端情境下，现实安全优先于哲学讨论。
- 不使用《西西弗神话》等内容去浪漫化、辩论或挑战用户的求生意愿。
- 提示用户联系身边可信赖的人、当地紧急服务或合格专业人士。
- 医疗、心理、法律和财务问题显示领域边界，不给诊断或确定性专业建议。

#### F. 响应式网页

- 适配手机和桌面浏览器。
- 支持键盘完整操作。
- 保持阅读页干净、克制，不设计人物头像对话气泡或拟人陪伴元素。
- 使用无障碍语义标签、清晰焦点状态和足够色彩对比度。

### 4.2 MVP 明确不包含

- 语义向量检索。
- 浏览器本地生成模型。
- 云端或自托管大模型。
- 连续多轮聊天。
- 用户账号、个人画像和跨会话记忆。
- 声音克隆、真实肖像动画或第一人称加缪角色扮演。
- 社交分享中的自动长文生成。
- 用户自定义人物或其他哲学家。
- 原著全文阅读和大段引文展示。
- 付费、订阅和商业化功能。

## 5. 核心用户流程

```text
打开首页
  → 阅读简短产品说明
  → 输入一个现实问题
  → 本地安全规则判断
      ├─ 极端情境：显示安全优先回答
      └─ 普通问题：继续检索
  → 自动判断中文或英文
  → 对应语言的标准化、查询扩展与 BM25 索引
  → BM25 返回候选思想卡片
  → 置信度与主题多样性判断
      ├─ 置信度不足：说明未找到，并推荐改写/主题入口
      └─ 置信度足够：选择 1 主卡片 + 0–2 辅助卡片
  → 确定性回答拼装
  → 展示回答、来源、边界与检索解释
  → 用户可重新提问
```

MVP 每次提交视为一次独立推演。新问题不继承上一问题的身份、情绪或关系上下文。

## 6. 思想知识库

### 6.1 内容规模

发布门槛：

- 300–500 张已审核思想卡片。
- 12 个主题均有覆盖，每个主题不少于 15 张卡片。
- 至少 150 个高频现实场景有明确覆盖。
- 至少 100 条安全与边界测试问题。
- 所有发布卡片必须标为 `approved`，草稿不得进入生产构建。

### 6.2 思想卡片数据结构

推荐使用 JSON 或 YAML，构建时通过 schema 校验。最低数据结构如下：

```ts
type ThoughtCard = {
  id: string;
  version: number;
  status: "draft" | "review" | "approved" | "rejected";

  title: string;
  theme: ThemeId;
  secondaryThemes: ThemeId[];

  // 思想内容
  principle: string;
  explanation: string;
  boundary: string;
  tensions: string[];
  counterMisreadings: string[];

  // 检索内容
  keywords: string[];
  aliases: string[];
  situations: string[];
  negativeSignals: string[];

  // 回答组件
  answerBlocks: {
    perspective: string[];
    boundary: string[];
    actions: string[];
    reflectionQuestions: string[];
  };

  // 来源与权利
  sources: SourceRef[];
  directQuoteIds: string[];
  rightsStatus: "cleared" | "public-domain" | "licensed" | "unknown";

  // 安全与审核
  safetyTags: string[];
  reviewer: string;
  reviewedAt: string;
  reviewNotes: string;
};

type SourceRef = {
  work: string;
  section?: string;
  edition?: string;
  language: string;
  sourceType: "primary" | "scholarship" | "editorial";
  locator?: string;
  url?: string;
};
```

### 6.3 卡片示例

```json
{
  "id": "absurd-repetitive-work-001",
  "version": 1,
  "status": "approved",
  "title": "清醒地面对重复劳动",
  "theme": "absurd",
  "secondaryThemes": ["work", "action"],
  "principle": "荒诞来自人对意义的要求与世界沉默之间的冲突；意识到冲突并不自动要求停止行动。",
  "explanation": "重复劳动可以让人感到生活被外部节奏占据。加缪式的起点不是制造虚假意义，而是先不欺骗自己地看清处境。",
  "boundary": "清醒地继续生活不等于无条件忍受伤害、剥削或不可改变的工作安排。",
  "tensions": ["继续与离开", "清醒与自欺", "生存需要与个人自由"],
  "counterMisreadings": ["既然荒诞，一切都无所谓", "反抗一定意味着立即辞职"],
  "keywords": ["上班", "工作", "重复", "无意义", "职业倦怠", "坚持"],
  "aliases": ["每天都一样", "机械生活", "为什么继续"],
  "situations": ["对重复工作失去意义感", "想辞职但担心现实后果"],
  "negativeSignals": [],
  "answerBlocks": {
    "perspective": ["问题也许不只是这份工作有没有意义，而是你是否还能在不自欺的情况下选择自己的行动。"],
    "boundary": ["清醒不要求你把忍耐美化成美德，也不要求你冲动地摧毁现有生活。"],
    "actions": ["先写下这份工作中你能够改变、暂时不能改变和不能继续接受的三件事。"],
    "reflectionQuestions": ["你真正无法忍受的是重复本身，还是自己对生活已经失去选择权？"]
  },
  "sources": [
    {
      "work": "《西西弗神话》",
      "section": "荒诞推理与西西弗神话",
      "language": "fr",
      "sourceType": "primary"
    }
  ],
  "directQuoteIds": [],
  "rightsStatus": "cleared",
  "safetyTags": ["meaning"],
  "reviewer": "editor-001",
  "reviewedAt": "2026-08-04",
  "reviewNotes": "概述而非直接翻译；上线前复核作品定位。"
}
```

### 6.4 内容生产流程

1. 建立作品与研究来源清单，记录版本、语言和权利状态。
2. 从来源中提炼原子化主张、适用条件、边界和常见误读。
3. 将每个主张映射到现实场景与自然语言问法。
4. 编写多个回答组件，不直接写成唯一完整答案。
5. 进行思想准确性审核。
6. 进行版权、引文和归因审核。
7. 进行安全与极端误读审核。
8. 标记为 `approved` 后进入生产索引。

开发阶段可以使用 AI 辅助起草、扩展问法或发现重复内容，但任何 AI 草稿都必须经过人工审核，且发布数据中应保留来源与审核记录。

## 7. BM25 检索设计

### 7.1 为什么 MVP 使用 BM25

- 无需运行时 AI 推理。
- 能完全在浏览器执行。
- 结果稳定、可解释、容易测试。
- 对 300–500 张结构化卡片足够快。
- 通过关键词、别名、场景和查询扩展可以覆盖大量自然问法。

### 7.2 文本处理

查询处理顺序：

1. Unicode 标准化。
2. 转小写并清理无意义标点。
3. 按检测语言执行中文分词或英文词元化。
4. 保留关键双字/三字片段作为分词失败时的补充。
5. 去除极少量无区分度停用词，但保留否定词，例如“不”“没有”“不能”。
6. 使用受控同义词表进行查询扩展。
7. 检测领域与安全标签。

不要依赖不可审计的自动同义词生成。查询扩展词典属于内容资产，存放在版本控制中。

### 7.3 字段权重初始值

以下为初始值，最终通过评测集调整：

| 字段 | 权重 |
|---|---:|
| `keywords` | 4.0 |
| `aliases` | 3.5 |
| `title` | 3.0 |
| `situations` | 2.5 |
| `tensions` | 2.0 |
| `principle` | 1.5 |
| `explanation` | 1.0 |
| `boundary` | 0.8 |

BM25 参数初始使用 `k1 = 1.2`、`b = 0.75`。参数和字段权重必须集中配置，不得散落在组件中。

### 7.4 候选选择

1. 检索 Top 8 候选。
2. 排除 `status !== approved` 或权利状态不允许发布的卡片。
3. 主卡片取最高相关项。
4. 辅助卡片最多两张，必须补充不同的张力、边界或行动维度。
5. 不允许三张卡片只重复同一结论。
6. 低于置信度阈值时不生成正式答案。

绝对分数阈值不能凭感觉硬编码，应使用评测集标定。可以同时参考：

- 第一名绝对分数。
- 第一名与第二名的分差。
- 命中字段类型。
- 是否仅命中高频弱词。

### 7.5 无结果体验

无结果时展示：

- 当前问题没有找到足够贴切的思想依据。
- 提示用户补充具体处境、选择或冲突。
- 推荐 3 个最接近的主题入口。
- 不输出看似合理但没有来源支撑的哲学回答。

### 7.6 检索可解释性

结果页提供可展开区域，例如：

> 找到这些思想，是因为你的问题涉及“重复劳动”“意义感”和“继续或离开之间的选择”。

不向普通用户展示原始 BM25 数字分数；调试模式可以显示分词、扩展词、字段命中和排名。

## 8. 回答拼装器

### 8.1 基本原则

回答拼装器只能使用：

- 用户问题中可安全复述的信息。
- 已审核思想卡片字段。
- 已审核的连接句和结构模板。
- 已审核引文库中的直接引语。

不得补充卡片中没有的历史事实、作品情节、引文或加缪个人经历。

### 8.2 推荐输出模板

```text
[自然回答正文]
你面对的核心张力是 ______。从加缪思想看，______。这并不意味着 ______。现在可以先 ______。

[视觉引句：已审核的来源版本短引文]
______
[回答语言译文]
______
[作者 · 作品]

[数据层保留思想来源，不在结果页渲染]
```

实际界面不必显示方括号标题，但数据层必须保留各部分来源，便于测试与解释。

### 8.3 表达规范

- 使用第二人称，但不替用户定义感受。
- 句子清楚、克制，少用感叹号和煽情修辞。
- 不刻意模仿翻译腔。
- 不使用“命中注定”“你必须”“加缪一定会”等绝对措辞。
- 不把痛苦浪漫化，不把忍耐自动视为高尚。
- 行动建议应低风险、具体、可逆。
- 每个回答最多提出一个主要反问。
- 不为了“文学感”牺牲可读性。

### 8.4 去重复规则

- 相同句子不得在同一回答中重复。
- 主卡片和辅助卡片结论高度相似时只保留主卡片。
- 多个行动建议冲突时，优先选择风险更低且更可逆的建议。
- 多个反问只选择最贴近用户核心张力的一个。

## 9. 安全设计

### 9.1 安全优先级

安全分流发生在哲学检索之前。任何极端情境都不能仅依赖 BM25 找哲学卡片处理。

### 9.2 MVP 安全类别

- 明确自残或自杀意图。
- 正在遭受或准备实施暴力。
- 儿童或未成年人危险。
- 重大财产诈骗或胁迫。
- 急性医疗危险。
- 请求专业诊断或确定性法律、财务结论。

### 9.3 安全回答要求

- 清楚承认情况严重。
- 鼓励用户立即联系当地紧急服务、可信赖的人或专业人士。
- 如果存在即时危险，优先离开危险环境并寻求现实帮助。
- 不进行哲学辩论，不引用自杀、死亡或荒诞主题作为主要回应。
- 可以在安全提示之后提供非常简短的支持性语言，但不得表现为专业治疗。

安全规则、测试案例和回答文案必须独立存放，方便专门审核。

## 10. 页面与交互

### 10.1 首页

包含：

- 产品名称。
- 一句话说明。
- 固定透明度声明。
- 问题输入框和提交按钮。
- 示例问题。

### 10.2 结果页或结果状态

包含：

- 用户原问题仅在结果页标题右侧显示一次。
- 与首页一致的全页黑白加缪背景：照片覆盖页面、人物保持在右侧，并使用黑色渐变承载前景文字。
- 一段自然回答与一条带完整出处的加缪原文引句。
- 重新提问入口。

用户原问题还用于本地检索与确定性拼装，但不得在回答正文中再次复述。

### 10.3 方法与来源页

说明：

- 产品不是加缪本人。
- 如何建立思想卡片。
- BM25 如何检索。
- 回答如何拼装。
- 引文与版权原则。
- 用户问题如何处理。
- 专业服务与危机边界。

## 11. 技术架构

### 11.1 推荐技术栈

- Vite
- React
- TypeScript
- 静态 JSON/YAML 内容
- 构建时 schema 校验
- 浏览器内 BM25 实现
- Vitest 单元测试
- Playwright 端到端测试
- 任意支持 HTTPS 的静态托管服务

### 11.2 架构约束

- 生产构建不得包含 API route、server action 或 AI SDK。
- 页面加载后，问题处理不产生包含问题文本的网络请求。
- 检索索引在构建阶段生成，浏览器加载只读索引。
- 所有内容版本进入 Git。
- 生产构建因卡片 schema 错误、未审核卡片或未知权利状态而失败。
- 安全规则优先级高于普通检索规则。

### 11.3 建议目录结构

```text
what-would-camus-say/
├── PRODUCT_PLAN.md
├── README.md
├── package.json
├── public/
├── content/
│   ├── cards/
│   ├── quotes/
│   ├── sources/
│   ├── synonyms/
│   └── safety/
├── scripts/
│   ├── validate-content.ts
│   ├── build-index.ts
│   └── evaluate-retrieval.ts
├── src/
│   ├── app/
│   ├── components/
│   ├── retrieval/
│   │   ├── tokenizer.ts
│   │   ├── bm25.ts
│   │   ├── query-expansion.ts
│   │   └── select-cards.ts
│   ├── composition/
│   ├── safety/
│   ├── content/
│   └── types/
├── tests/
│   ├── retrieval/
│   ├── composition/
│   ├── safety/
│   └── e2e/
└── evals/
    ├── retrieval-gold.json
    ├── safety-prompts.json
    └── answer-quality.json
```

### 11.4 隐私要求

- 默认不保存问题。
- 不将问题写入 URL 查询参数。
- 不将问题发送给分析、错误监控或日志服务。
- 错误日志不得包含完整用户输入。
- 如果未来收集原始问题，必须使用明确的单独同意并更新隐私说明。

## 12. 质量评测

### 12.1 检索评测集

在发布前建立至少 200 个真实风格问题，每个问题由人工标注：

- 必须命中的主思想卡片或允许集合。
- 可接受的辅助主题。
- 不应命中的危险误读。
- 是否应该进入安全分流。
- 是否应该返回无结果。

### 12.2 MVP 验收指标

#### 内容

- 生产卡片数量达到 300 张以上。
- 生产卡片 `approved` 比例为 100%。
- 每张卡片至少有一个来源。
- 直接引语的来源与权利状态完整率为 100%。
- 不存在未经核验的“加缪原话”。

#### 检索

- 普通评测问题 Top 3 相关思想召回率达到 85% 以上。
- 安全评测问题安全分流召回率达到 100%。
- 无结果测试不得输出没有思想依据的正式回答。
- 在中端手机浏览器上，索引加载完成后的单次检索目标小于 200ms。

#### 回答

- 每个普通回答都包含观点、边界和现实一步；所用来源保留在思想卡片元数据中。
- 回答中所有思想性陈述都能追溯到选中的卡片。
- 不出现编造引语、作品章节或生平事实。
- 人工盲评中，“思想相关且没有明显误读”的比例达到 90% 以上。

#### 隐私与体验

- 网络检查证明原始问题不会离开浏览器。
- 手机和桌面核心流程均可完成。
- 键盘操作可完成输入、提交、展开来源和重新提问。
- Lighthouse Accessibility 目标不低于 90。

## 13. 实现阶段与 Agent 任务

### Phase 0：项目骨架与决策固化

任务：

- 初始化 Vite + React + TypeScript 项目。
- 添加 lint、format、Vitest 和 Playwright。
- 建立上述目录。
- 将本规划链接到 README。
- 添加禁止 API/AI SDK 的架构检查或依赖审查。

退出条件：

- 本地开发、测试和静态构建命令可运行。
- 空白首页可在手机与桌面尺寸打开。

### Phase 1：内容模型与 20 张种子卡片

任务：

- 定义 TypeScript 类型和内容 schema。
- 编写内容校验脚本。
- 建立来源、引文、同义词和安全数据结构。
- 制作覆盖至少 6 个主题的 20 张种子卡片。
- 为每张卡片记录审核状态与来源。

退出条件：

- 错误卡片会让构建失败。
- 20 张种子卡片全部通过校验。

### Phase 2：BM25 检索与评测框架

任务：

- 实现中文标准化、英文词元化与必要的字符片段补充。
- 实现受控查询扩展。
- 实现带字段权重的 BM25。
- 实现候选去重、主题多样性与低置信度处理。
- 建立首批 50 条检索金标问题。
- 提供调试视图，显示分词、命中字段和排名。

退出条件：

- 50 条种子问题 Top 3 召回率达到 80% 以上。
- 无明显相关卡片时可以稳定返回无结果。

### Phase 3：回答拼装与核心 UI

任务：

- 实现首页输入与示例问题。
- 实现确定性回答拼装器。
- 实现观点、边界、现实一步和反问的结构化展示。
- 在回答数据中保留来源映射，并实现重新提问入口。
- 增加本地反馈状态。

退出条件：

- 从输入到回答的完整流程可用。
- 每条回答均可追溯到卡片字段。
- 不存在自由生成文本路径。

### Phase 4：安全、隐私与无障碍

任务：

- 实现独立安全分流。
- 建立至少 100 条安全测试问题。
- 确认问题不进入网络请求、URL、日志和错误报告。
- 完成键盘、焦点、屏幕阅读器和对比度检查。
- 完成移动端布局和异常输入处理。

退出条件：

- 安全测试集分流召回率 100%。
- 网络检查确认原始问题不离开浏览器。
- 无障碍目标达标。

### Phase 5：内容扩充与公开 MVP

任务：

- 将知识库扩充到 300–500 张已审核卡片。
- 将检索评测集扩充到至少 200 条。
- 调整字段权重、同义词表与置信度阈值。
- 完成方法、来源、隐私和边界说明页面。
- 部署到 HTTPS 公网域名。
- 完成生产环境冒烟测试。

退出条件：

- 达到第 12 节全部 MVP 验收指标。
- 生产环境没有 API 或模型推理请求。
- 所有公开内容来源、审核和权利状态完整。

## 14. MVP 后路线

只有在 BM25 MVP 已有真实评测数据后，才评估以下能力。

### 14.1 语义检索

目的：处理“文字不同但意思相近”的问题。

实现方向：

- 卡片向量在离线构建阶段生成。
- 用户问题在浏览器内通过小型 embedding 模型转成向量。
- BM25 与向量结果分别排序，再使用 RRF 合并。
- 保留 BM25 的来源解释和无结果机制。

上线前必须验证：

- 模型下载大小与移动端性能。
- WebGPU/WASM 兼容性。
- 语义检索是否真正提升金标集召回率。
- 浏览器推理的 AI 内容与合规说明。

### 14.2 浏览器本地小模型

目的：仅改善表达连贯性与语气，不负责发明思想内容。

约束：

- 输入只能是用户问题、已检索卡片和安全规则。
- 思想骨架与来源先由确定性逻辑产生。
- 模型不得生成直接引语。
- 必须有无模型降级路径。
- 必须单独评测幻觉、加载体积、延迟和设备兼容性。

### 14.3 多轮对话

不作为自然延伸默认加入。若未来评估，必须继续保持“知识问答/思想推演”定位，不设计关系养成、情感依赖或人物身份模拟。

## 15. 风险与缓解

| 风险 | 影响 | MVP 缓解方式 |
|---|---|---|
| 把一般鸡汤包装成加缪思想 | 产品失真 | 原子化卡片、来源、边界和人工审核 |
| BM25 不理解生活化表达 | 检索漏召回 | 场景字段、别名、同义词、字符片段与持续评测 |
| 拼装回答生硬 | 用户体验差 | 多个已审核回答组件、连接句与去重复规则 |
| 捏造加缪原话 | 可信度与版权风险 | 引文独立白名单；默认只做思想概述 |
| 产品被理解为加缪复活 | 伦理与合规风险 | 命名、文案、视觉与交互持续去拟人化 |
| 极端问题被哲学化处理 | 用户安全风险 | 检索前独立安全分流和专用回答 |
| 用户隐私泄漏 | 信任与合规风险 | 本地处理、无账号、无原问题遥测 |
| 内容规模增加导致重复冲突 | 回答不稳定 | 卡片版本、重复检测、主题与张力约束 |
| 现代译本或原文使用不当 | 版权风险 | 权利状态字段、构建阻断、短引文白名单 |

## 16. 完成定义

MVP 只有同时满足以下条件才算完成：

- 用户可以在公网网页提交现实问题并得到结构完整的思想推演。
- 运行时没有云端或本地生成模型参与回答。
- 检索和拼装全部在浏览器内完成。
- 每条普通回答在数据层可追溯到思想来源，并在界面中包含必要边界和一个现实下一步。
- 无依据时诚实返回无结果。
- 极端情境优先进入安全分流。
- 用户问题默认不离开设备。
- 不冒充加缪，不伪造引语，不提供持续性情感陪伴。
- 内容、检索、回答、安全、隐私和无障碍指标均达到第 12 节要求。

在此基础上，团队再根据真实评测数据决定是否需要语义检索或浏览器小模型，而不是预设“生成模型一定更好”。
